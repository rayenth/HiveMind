apiVersion: 1

datasources:
  - name: MySQL
    type: mysql
    access: proxy
    url: mysql:3306
    user: grafana
    secureJsonData:
      password: grafana_pass
    database: grafana_db
    isDefault: true
