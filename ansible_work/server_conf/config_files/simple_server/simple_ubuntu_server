FROM ubuntu:22.04

ENV DEBIAN_FRONTEND=noninteractive
WORKDIR /data

RUN apt-get update && \
    apt-get install -y --no-install-recommends \
        iputils-ping \
        net-tools \
        curl \
        vim \
        sudo && \
    rm -rf /var/lib/apt/lists/*

CMD ["tail", "-f", "/dev/null"]